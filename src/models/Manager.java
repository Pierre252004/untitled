package src.models;

public class Manager extends Employee {
    public Manager(String ssn, String fullName, String hotelAddress, double salary) {
        super(ssn, fullName, hotelAddress, salary);
    }
}